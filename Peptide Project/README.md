# Name-to-Peptide Studio

A small static web app that turns a name or surname into:

- A one-letter peptide sequence
- A three-letter amino-acid sequence
- A peptide molecular formula
- An average molecular weight
- An average peptide charge at a user-selected pH
- A 2D peptide structure formula as an SVG line drawing
- A rotatable peptide study model rendered from an RDKit molfile in the browser

## How the mapping works

The app keeps only alphabetic characters, normalizes accented letters, and maps each remaining letter to a standard amino-acid one-letter code.

- Direct matches stay the same. Example: `P -> P`
- Non-standard letters fall back to the closest standard amino-acid code alphabetically
- If two standard codes are equally close, the earlier one alphabetically wins

Examples:

- `B -> A`
- `J -> I`
- `O -> N`
- `U -> T`

## Charge calculation

The pH-based average charge uses the pKa table you provided and includes:

- The N-terminus alpha-amino group of the first residue
- The C-terminus alpha-carboxyl group of the last residue
- Ionizable side chains for Asp, Glu, Cys, Tyr, His, Lys, and Arg

The calculation uses Henderson-Hasselbalch style fractional protonation, so the result is an average charge rather than an all-or-nothing integer.

## What the 3D model means

The app now builds the peptide from RDKit-generated chemistry data and loads the resulting molfile into Mol*.

- Bond orders and atom connectivity come directly from the peptide SMILES, so the viewer no longer guesses bonds from a hand-built fake backbone
- This is still a study model, not a protein-structure prediction, force-field minimization, or a solvated simulation
- The coordinates are autogenerated for viewing convenience, so the preview is chemically connected but not intended to represent a physically optimized fold

The 3D panel now reports the source of the chemistry data and the bond-topology mode instead of a confidence score.

## Run locally

Because this is a static site, you can:

1. Open [index.html](C:\Users\gkcel\Desktop\HTS Lab\Lab Website\Lab Website - Main\Peptide Project\index.html) directly in a browser, or
2. Serve the folder with any simple static server

The 3D view requires internet access for:

- The `Mol*` viewer script and stylesheet

The 2D structure formula uses bundled local RDKit.js assets from `vendor/rdkit`, so it does not depend on an external chemistry drawing CDN.

## Files

- [index.html](C:\Users\gkcel\Desktop\HTS Lab\Lab Website\Lab Website - Main\Peptide Project\index.html): page structure
- [styles.css](C:\Users\gkcel\Desktop\HTS Lab\Lab Website\Lab Website - Main\Peptide Project\styles.css): layout and visual design
- [app.js](C:\Users\gkcel\Desktop\HTS Lab\Lab Website\Lab Website - Main\Peptide Project\app.js): peptide mapping, formula calculation, 2D structure generation, RDKit molfile generation, and 3D model rendering
